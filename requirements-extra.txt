# FigTabMiner - 增强功能依赖
# 这些依赖提供额外的功能和更好的准确率

# OCR 支持（用于文本识别）
easyocr>=1.7.0

# 表格提取增强
camelot-py[cv]>=0.11.0
ghostscript>=0.7

# 布局检测 - PubLayNet（基础）
layoutparser>=0.3.4
detectron2

# 布局检测 - DocLayout-YOLO（v1.5，推荐）
doclayout-yolo

# 表格检测 - Table Transformer（v1.6，推荐）
transformers>=4.30.0
torch>=2.0.0
torchvision>=0.15.0
